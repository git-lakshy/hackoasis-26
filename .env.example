GROQ_API_KEY=your_groq_api_key_here
SLACK_WEBHOOK_URL=
NOTIFY_EMAIL=
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Set to "real" to use live AWS data instead of mock
CLOUD_MODE=mock

# AWS credentials (required when CLOUD_MODE=real)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
